type: markdown
content: <font size="5px"><b>{{ states('sensor.greeting') }}, {{user}}</b></font>    # '{{user}}' will take the name of the signed in user in Home Assistant.
card_mod:
  style: |
    ha-card {
    background: none;
    box-shadow: none;
    border: none;
    }
